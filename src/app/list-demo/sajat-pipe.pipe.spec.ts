import { SajatPipePipe } from './sajat-pipe.pipe';

describe('SajatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SajatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
